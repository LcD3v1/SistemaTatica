# Dependências
node_modules/
**/node_modules/

# Builds compilados
dist/
**/dist/

# Variáveis de ambiente (contêm segredos)
.env
**/.env
.env.local
**/.env.local
.env.*.local
**/.env.*.local
.env.secbak-*
**/.env.secbak-*
# (.env.example e .env.production — sem segredos — permanecem versionados)

# Dados persistidos em disco (não versionar dados de produção)
backend/data.json
# Backups de dados/segredos gerados localmente
backend/data.json.*
**/*.bak
**/*.bak-*

# Log de auditoria de segurança
backend/audit.log
*.log

# Chaves de serviço
serviceAccountKey.json
**/serviceAccountKey.json

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code
.claude/

# Mídia pesada acima de 90MB deve ser comprimida ou hospedada via Git LFS
# (o login-bg.mp4 foi comprimido para ~28MB e agora é versionado normalmente)

# SO
.DS_Store
Thumbs.db
